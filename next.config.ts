import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    domains: ['placehold.co', 'example.com'], // Add any other image domains you need
  },
  /* config options here */
}

export default nextConfig
